<%= render 'search'%>

<% if @pages != []%>
<% @pages.each do |page| %>

<h1><%= link_to page.title, page_path(page) %></h1>
<p>Author: <%= link_to page.user.username, user_path(page.user) %></p>

<% end %>

<%else%>
<h1>We could not find any pages!</h1>
<%end%>
